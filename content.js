//js контента (хз)
/*var sendLead = (function() {
    var exposed = {
        //проверка текущей страницы
        checkPage : function()
        {
            return 'page is page';
        }
    };
    return exposed;
})();*/

function checkPage()
{
    return 'page is page';
}